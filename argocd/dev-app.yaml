apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-template-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/kube-kr-aayush/gitops-app-template.git
    targetRevision: main
    path: overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
